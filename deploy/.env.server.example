# Docker Compose infrastructure settings. Store as /opt/hrms/.env.
# Use an alphanumeric password because it is interpolated into DATABASE_URL.
POSTGRES_PASSWORD=replace-with-a-long-random-alphanumeric-password
